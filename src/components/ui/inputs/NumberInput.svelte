<!-- <script lang="ts">
	import type { InputProps } from './input-types';

	let props: InputProps = $props();

	let { id, onChange } = props;
	console.log(onChange);
	let value = $derived(props.value);
</script>

<input {id} name={id} type="number" onchange={onChange} {value} /> -->
